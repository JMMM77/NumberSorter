import {
  ROOT_PATHS,
  SORTED_RESULTS_PATHS,
} from './paths.constants';

export const ROOT_URLS = {
  home: `${ROOT_PATHS.home}`,
};

export const SORTED_RESULTS_URLS = {
  base: `${SORTED_RESULTS_PATHS.base}`,
};